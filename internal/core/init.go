package core

/*
 * func init() {
 *     deck = NewDeck()
 *
 *     // populate originMatrix
 *     for i := range origin {
 *         originMatrix.cards[i] = deck.cards[origin[i]-1]
 *     }
 *
 *     // build the matrices
 *     matrices[0] = originMatrix
 *
 *     for i := 1; i < 90; i++ {
 *         prev := matrices[i-1]
 *
 *         if matrices[i], err = prev.next(); err != nil {
 *             log.Fatalf("Unable to build matrix for year: %v", i)
 *         }
 *     }
 *     // fmt.Println(matrices)
 *
 *     // create aliases for matrices of angels and humans
 *     // FIXME: 44th is angels matrix
 *     // FIXME: 45th is humans matrix
 *     angelsMatrix = matrices[44]
 *     humansMatrix = matrices[45]
 * }
 */
