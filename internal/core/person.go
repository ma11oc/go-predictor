package core

/*
 * import (
 *     "fmt"
 *     "time"
 * )
 *
 * // func (c card) getPersonalCards(t *time.Time) [3]*card {
 * // 	// TODO: depends on gender and birthday
 * // 	return [3]*card{}
 * // }
 *
 * func (c Card) GetDrainCard() *Card {
 *     i, err := humansMatrix.indexOf(uint8(c.Order))
 *     if err != nil {
 *         fmt.Println("unable to find drain card")
 *     }
 *
 *     return angelsMatrix.cards[i+1]
 * }
 *
 * func (c Card) GetSourceCard() *Card {
 *     i, err := angelsMatrix.indexOf(uint8(c.Order))
 *     if err != nil {
 *         fmt.Println("unable to find source card")
 *     }
 *
 *     return humansMatrix.cards[i+1]
 * }
 *
 * func GetCardByBirthday(t *time.Time) *Card {
 *     i := uint8(54 - (t.Day() + (int(t.Month()) * 2)))
 *
 *     return deck.cards[i+1]
 * }
 *
 * // TODO: personal cards
 * // TODO: drain and source cards
 * // TODO: long-term card
 * // TODO: year layout
 */
